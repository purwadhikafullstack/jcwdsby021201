import * as React from 'react';
import {
  Grid,
  Typography,
  Select,
  MenuItem,
  SelectChangeEvent,
} from '@mui/material';
interface IAddressSelectionProps {}

const AddressSelection: React.FunctionComponent<IAddressSelectionProps> = (
  props,
) => {
  return <div>AAA</div>;
};

export default AddressSelection;
